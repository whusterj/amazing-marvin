# Direct dependencies only. Let pip resolve the rest.
#
# This file held the output of `pip freeze` until August 2026. That pinned
# every transitive package too, which kept about a hundred stale versions
# alive and raised a security alert for each one.

# API and database access
httpx>=0.28
ibmcloudant>=0.10
python-dotenv>=1.0

# Notebook and plotting
ipympl>=0.9.4
jupyter>=1.1
matplotlib>=3.9
mplcursors>=0.6
numpy>=2.1

# Tests and code quality
black>=25.1
flake8>=7.1
pytest>=8.3
pytest-asyncio>=0.25
